'use client'
import { createTheme } from '@mui/material/styles'

export default createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#c90620'
        }
    },

    typography: {
        fontFamily: 'Inter, sans-serif',
    }
})